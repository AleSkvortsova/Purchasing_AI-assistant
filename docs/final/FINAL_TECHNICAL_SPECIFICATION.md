# Итоговое техническое задание

## ИИ-ассистент по внутренним заявкам на закупку

**Версия:** 2.0, по фактически реализованному MVP
**Дата актуализации:** 15 августа 2026 года
**Автор исходного ТЗ:** Скворцова Александра
**Интерфейс:** Telegram-бот «Закупкин»
**Production release:** `06f66e2906e48d46dec42a09a4f459bd1dd01593`
**Статус:** MVP принят по результатам финального функционального и production smoke testing с известными ограничениями

## 1. Назначение документа

Документ заменяет плановое описание версии 1.0 от 17.07.2026 итоговым ТЗ по реально работающему MVP. Он отдельно фиксирует исходный замысел, изменения в ходе разработки и окончательные границы продукта. Наличие роли, таблицы или программного модуля не считается доказательством готового пользовательского сценария.

## 2. Контекст и проблема

Решение рассчитано на среднюю дистрибьюторскую компанию примерно со 100 сотрудниками, где нерегулярные закупки товаров и услуг инициируют маркетинг, продажи, логистика, склад, IT, финансы и АХО.

Исходная проблема:

- запросы поступают через почту, мессенджеры, документы и устные сообщения;
- 30–40% заявок могут быть неполными;
- в отделе закупок такие заявки обрабатывают два сотрудника, при потоке порядка 15–20 заявок в день;
- приходится уточнять характеристики, количество, сроки, бюджет и обоснование;
- часть запросов теряется или не фиксируется;
- внутренний заказчик не видит единый статус, а процесс трудно контролировать.

## 3. Исходный замысел

### 3.1. Цель версии 1.0

Создать ассистента, который превращает свободное описание товара или услуги в полную структурированную заявку: определяет тип и категорию, запрашивает недостающие сведения, проверяет обязательные поля, показывает карточку и передаёт подтверждённую заявку в отдел закупок.

### 3.2. Планировавшиеся роли и сценарии

Исходное ТЗ описывало три группы участников:

1. внутренние заказчики;
2. сотрудники отдела закупок;
3. участники согласования и контроля.

Также планировались возврат заявки закупщиком на доработку, изменение статусов, уведомления и запрос подробного статуса. Эти функции не следует считать готовыми только потому, что они присутствовали в замысле.

### 3.3. Плановая архитектура

Планировались Telegram, Python/FastAPI, Supabase PostgreSQL, pgvector, RAG и вспомогательный n8n. Google Sheets упоминался как реестр для части учебных сценариев, Docker — как возможный компонент размещения.

## 4. Изменения по ходу разработки

| Область | План | Фактическое решение |
|---|---|---|
| Реестр заявок | Google Sheets / единый реестр | Supabase PostgreSQL и транзакционные RPC |
| Развёртывание | Docker упоминался в плане | Python virtual environment и systemd на VPS |
| Диалог | Последовательное заполнение | Сохраняемый многошаговый черновик, конфликты, возврат к редактированию и восстановление после навигации |
| Извлечение данных | LLM-классификация и вопросы | Комбинированное извлечение: OpenAI Structured Outputs плюс программный разбор, нормализация и резервный режим |
| Ответы по правилам | База знаний / RAG | Отдельный Regulation Q&A с гибридным поиском, источниками, проверкой утверждений и многошаговыми уточнениями |
| Согласование | Возможный автоматический маршрут | Реализован только предварительный детерминированный расчёт маршрута; фактического согласования нет |
| История | Запрос статуса | Пять последних зарегистрированных заявок владельца в режиме просмотра |
| База знаний | 8–10 документов и 15–20 примеров | 14 активных документов и production-индекс из 118 чанков / 118 embeddings |
| Категории | Определение по классификатору | Deterministic exact, затем closed-taxonomy LLM при отсутствии strong result; application validation, явное подтверждение и readiness guard |

## 5. Итоговые границы MVP

### 5.1. Реализовано

- русский Telegram-интерфейс;
- оформление одной основной закупочной потребности как товара или услуги;
- извлечение уже указанных сведений из свободного текста;
- запрос одного следующего недостающего или конфликтующего поля;
- определение или уточнение категории по закрытой taxonomy G01–G15 / S01–S15
  версии `intake-categories-v3`;
- разные требования к товару и услуге;
- нормализация количества, единиц, суммы, даты и бюджетного статуса;
- карточка перед отправкой;
- один активный сохраняемый черновик пользователя;
- возврат к редактированию и изменение значения свободным текстом;
- отмена черновика;
- регистрация только после явного подтверждения;
- уникальный номер `PR-YYYY-NNNNNN` и статус «Передана в отдел закупок» (`new`);
- пять последних зарегистрированных заявок владельца;
- отдельный режим вопросов по регламентам с найденными источниками;
- сохранение короткого контекста уточнения по регламенту;
- предварительный маршрут согласования по программным правилам;
- HTTP API и Swagger для технических операций;
- эксплуатационный запуск Telegram-бота на VPS.

### 5.2. Частично реализовано

- разделение нескольких потребностей работает для отдельных проверенных сценариев, включая «лицензии + установка», но не гарантируется для произвольного текста;
- категории определяются или предлагаются как 2–3 кандидата, но общие описания могут давать слишком широкий список;
- редактирование возможно свободным текстом, но нет выбора конкретного поля кнопкой;
- история существует, но ограничена пятью записями без фильтров и подробной карточки;
- роли `buyer` и `admin` существуют в модели данных, но не имеют отдельных пользовательских интерфейсов.

### 5.3. Не реализовано

- кабинет закупщика и очередь обработки;
- возврат зарегистрированной заявки закупщиком на доработку;
- фактическое согласование, решения согласующих и уведомления;
- фактическая проверка бюджета;
- выбор поставщика, запрос предложений, тендер, договор, заказ и оплата;
- ERP/1С/CRM/WMS и складские интеграции;
- файлы, изображения и голосовой ввод;
- SSO и полная ролевая модель;
- аналитические панели;
- построчная спецификация нескольких позиций;
- промышленная готовность, RLS для реального пользовательского доступа и доказанный SLA.

## 6. Пользователи и роли

### 6.1. Внутренний заказчик — реализовано

Может создать, уточнить, проверить, подтвердить или отменить собственную заявку, восстановить активный черновик, просмотреть краткую историю и задать вопрос по регламенту.

### 6.2. Закупщик — только модель и бизнес-контекст

Роль `buyer` существует, но кабинета, очереди, комментариев и изменения статусов нет. Статус «Передана в отдел закупок» подтверждает регистрацию, а не фактическое принятие закупщиком.

### 6.3. Администратор — только эксплуатационная роль

Роль `admin` существует в модели. Настройка, обновление, журналы и база знаний обслуживаются на сервере. Отдельного административного интерфейса нет.

### 6.4. Согласующие — не реализовано

Руководители, финансовая служба и другие роли могут появиться в предварительном маршруте, но не получают заданий и не принимают решения в MVP.

## 7. Функциональные требования к итоговому MVP

### FR-01. Создание черновика

Система должна принимать свободное русскоязычное описание, создать не более одного активного черновика на пользователя и сохранить его между сообщениями и перезапуском процесса.

### FR-02. Извлечение и уточнение

Система должна извлекать только явно сообщённые данные, проверять их формат программными правилами, не переносить неподтверждённые значения из контекста и задавать один следующий вопрос.

### FR-03. Категория

Система должна выбрать безопасное deterministic exact совпадение. Если strong
результат отсутствует, допускается closed-taxonomy LLM-предложение: оно должно
пройти application validation и явное подтверждение пользователя. Только после
этого категория становится readiness eligible. Модель не может придумать новый
код, а unconfirmed, weak или stale suggestion не может быть зарегистрировано.
При нескольких вариантах показываются 2–3 сохранённых кандидата; нельзя
запрашивать номер до фактического показа списка.

### FR-04. Карточка

После заполнения обязательных полей система должна показать структурированную карточку. Для MVP карточка представляет один основной предмет с одним количеством, единицей и набором характеристик.

### FR-05. Явное подтверждение

Свободный текст на экране проверки не должен регистрировать заявку. Регистрация разрешена только отдельной командой подтверждения для актуальной версии черновика.

### FR-06. Регистрация и история

Подтверждённая заявка должна получить номер `PR-YYYY-NNNNNN`, перейти в `new` и появиться в истории владельца. Черновики и отменённые записи не должны отображаться как зарегистрированные.

### FR-07. Regulation Q&A

В справочном режиме система должна искать активные документы, отвечать только по найденным фрагментам, приводить понятные источники, запрашивать недостающие параметры и отказываться от ответа при недостатке нормативного контекста.

### FR-08. Изоляция режимов

Переход в справочный режим не должен повреждать активный черновик заявки.

## 8. Архитектура и технологии

```text
Telegram / aiogram, long polling
  → адаптер Telegram
  → OpenAI Structured Outputs + программный разбор
  → объединение относительно canonical accumulated draft context
  → нормализация и проверка полноты
  → deterministic exact category
  → при отсутствии strong result: closed-taxonomy LLM
  → application validation → явное подтверждение → readiness
  → сохранение шага через Supabase RPC / PostgreSQL
  → подтверждение, редактирование или отмена
```

```text
Вопрос по регламенту
  → понимание вопроса и недостающих параметров
  → векторный + русский полнотекстовый поиск
  → RRF, пять лучших фрагментов
  → ответ модели по контексту
  → программная проверка утверждений, значений и источников
```

Основные технологии: Python 3.11–3.12, aiogram 3, FastAPI, Pydantic, OpenAI Responses / Structured Outputs, `text-embedding-3-small` (1536 измерений), Supabase PostgreSQL, pgvector/HNSW, русский полнотекстовый поиск, pytest, Ruff, Git/GitHub, VPS и systemd.

Модель по умолчанию в конфигурации извлечения и ответов — `gpt-5.6-luna`.
Category provider использует модель, настроенную через application settings.
Это параметры конфигурации, а не бизнес-правила.

Модель таблиц, RPC, состояния, индексации, поиска и deployment описана в
[`../technical/TECHNICAL_ARCHITECTURE.md`](../technical/TECHNICAL_ARCHITECTURE.md).

## 9. Разделение ответственности LLM и кода

LLM используется для понимания свободного текста, структурированного извлечения и формулирования ответа по найденным фрагментам. Модель не принимает решения о регистрации, статусе, полноте, допустимом переходе, нумерации или согласующих.

Для категории действует отдельная безопасная последовательность:

```text
deterministic exact
  → если strong result отсутствует: closed-taxonomy LLM
  → application validation
  → explicit user confirmation
  → readiness eligible
```

Category provider получает только допустимые коды текущего типа и накопленный
семантический контекст canonical draft. Код и semantic context fingerprint
связывают подтверждение с предметом, типом и релевантным описанием. Изменение
этого контекста инвалидирует stale suggestion. Ошибка provider оставляет
категорию unresolved; deterministic known category не требует LLM-вызова.

Программный код определяет:

- допустимые поля и значения;
- нормализацию чисел, денег, дат, единиц и бюджетного статуса;
- обязательность полей;
- приоритет подтверждённых значений и конфликты;
- категориальные кандидаты и допустимый выбор;
- следующий вопрос и готовность карточки;
- явность подтверждения;
- жизненный цикл, версии, идемпотентность и номер;
- числовые диапазоны предварительного маршрута согласования;
- допустимость источников и конкретных значений ответа по регламенту.

При определении `procurement_type` deterministic parser является одним из
источников, а structured extraction используется для семантического понимания.
Сильный явный deterministic сигнал сохраняется при structured `null` или
rejection, но слабый контекстный маркер не должен ошибочно доминировать над
подтверждённым смыслом. Merge выполняется относительно накопленного canonical
draft context.

## 10. Данные и безопасность

Основные сущности: `users`, `requests`, `dialog_states`, `message_logs`, `knowledge_documents`, `knowledge_chunks`, таблицы правил согласования и команды жизненного цикла.

`dialog_states.current_intent` принимает `idle`, `intake` или
`regulation_qa`. Команда `/start` переводит режим в `idle` и очищает pending
Regulation context, не удаляя активный intake draft. «Новая заявка» включает
`intake`, а вход в Regulation menu создаёт чистый regulation context. Category
candidates и pending state сохраняются между экземплярами adapter; изменение
semantic context инвалидирует stale category suggestion.

Требования:

- учебный MVP использует синтетические данные;
- секреты не хранятся в коде;
- ключ Supabase `service_role` используется только сервером;
- чтение и изменение заявки проверяют владельца;
- критичные RPC доступны только серверной роли;
- шаги оформления и жизненный цикл защищены версиями и идемпотентностью;
- полные вопросы и ответы режима регламентов не должны попадать в технический журнал;
- до реального пилота обязательны RLS, политика хранения данных, ротация секретов и отдельная проверка безопасности.

## 11. Нефункциональные требования: план и факт

| Требование | План | Факт / подтверждение |
|---|---|---|
| Язык | Русский | подтверждено |
| Первый ответ | ≤10 секунд для 95% | не измерено на репрезентативной нагрузке |
| Сохранение подтверждённой заявки | ≤5 секунд | функционально работает; p95 не измерен |
| До 10 активных пользователей | план пилота | нагрузочным тестом не подтверждено |
| До 30 заявок и 500 сообщений в день | план | длительной эксплуатацией не подтверждено |
| Не регистрировать без подтверждения | обязательно | подтверждено ручным контрольным тестом |
| Сохранить черновик при ошибке | обязательно | базовое сохранение и восстановление подтверждены; аварийные режимы полностью не испытаны |
| Логирование и прослеживаемость | обязательно | реализованы события, версии и жизненный цикл; полный эксплуатационный аудит не проводился |
| Доступность 99% | только промышленная цель | к MVP не применяется и не измерялась |

## 12. План и факт реализации

| Этап исходного плана | Плановый срок | Фактический результат |
|---|---:|---|
| Подготовка | 1 неделя | ТЗ и база знаний подготовлены; база расширена до 14 рабочих документов |
| Разработка | 1 неделя | Реализованы Telegram, ядро оформления, Supabase, RAG, API и правила |
| Тестирование | 1 неделя | Выполнены автоматические наборы, ручные проверки и приёмка на VPS |
| Доработка и запуск | 1 неделя | Выполнены rehearsal, стабилизация, синхронизация индекса, финальное развёртывание и production smoke testing |

Исходное ТЗ датировано 17.07.2026, итоговая фиксация после production
стабилизации — 15.08.2026. Фактический табель трудозатрат не представлен;
100 часов и 150 000 ₽ остаются оценкой, а не подтверждённым фактом.

Финальный production release —
`06f66e2906e48d46dec42a09a4f459bd1dd01593`. Сервис
`purchasing-ai-bot.service` активен, Telegram polling запущен, production
working tree и startup logs проверены. Индекс содержит 14 активных документов,
118 чанков и 118 embeddings модели `text-embedding-3-small`; чанков без
embedding нет.

## 13. Критерии приёмки: плановые KPI и результат

| Критерий / KPI | План | Фактический результат | Статус |
|---|---:|---|---|
| Определение «товар/услуга» | ≥90% | проверенные ручные сценарии пройдены; отдельная репрезентативная метрика не рассчитана | требует дополнительного измерения |
| Определение категории | ≥85% в критериях приёмки; ≥90% в KPI | deterministic + closed-taxonomy LLM flow, подтверждение и lifecycle guard проверены regression и production smoke; универсальная точность не доказана | подтверждено функционально на проверенных сценариях |
| Выявление обязательных полей | ≥90% | пройдено на тестовых и ручных сценариях; отдельная итоговая доля не опубликована | частично подтверждено |
| Заявки без дополнительных уточнений закупщика | ≥80% | пилот с закупщиками не проводился | не подтверждено |
| Снижение неполных заявок | до 10–15% | пилот не проводился | не подтверждено |
| Снижение трудозатрат на уточнение | ≥50%, 4–6 часов в неделю | расчётный сценарий, не фактическое измерение | не подтверждено |
| Формирование стандартной заявки | ≤7 минут | сценарии завершены, но статистика времени не собиралась | не подтверждено метрикой |
| Регистрация всех подтверждённых заявок | 100% | подтверждено на контрольных заявках; универсальная эксплуатационная доля не измерялась | подтверждено функционально |
| Не регистрировать без подтверждения | 100% | фразы «пока не отправляй» и «не отправляй пока» не создали заявку | подтверждено |
| Уникальный номер и сохранение | обязательно | регистрация подтверждена на нескольких контрольных сценариях; в финальном production smoke заявка с мониторами зарегистрирована как `PR-2026-000035`; используется PostgreSQL sequence | подтверждено |
| Ответы по базе знаний | ≥90% | финальный baseline: Regulation domain 17/17, conversations 17/17, holdout 25/25; example leakage 0, unsupported concrete values 0 | подтверждено на фиксированных наборах |
| Не выдумывать конкретные значения | обязательно | example leakage = 0; unsupported concrete values = 0 | подтверждено на фиксированных наборах |
| Тесты без технической ошибки | ≥90% | 1375 тестов пройдено; одна существующая Starlette deprecation warning; Ruff и `git diff --check` прошли | подтверждено для набора тестов |
| Полезность пилотной группы | ≥8/10 | пилотная оценка отсутствует | не подтверждено |
| Пользовательская инструкция | обязательна | подготовлено отдельное [`USER_GUIDE.md`](../guides/USER_GUIDE.md), рабочая инструкция также входит в базу знаний | готово |
| Инструкция сотрудника закупок | обязательна | отдельная инструкция отсутствует, поскольку кабинет закупщика не реализован | не готово |
| Актуализация базы знаний | обязательна | владелец, версия, дата, статус, приоритет и ежеквартальная проверка определены | подтверждено процессом |

Высокие результаты фиксированных наборов не означают 100% точность на любых будущих сообщениях.

## 14. Бюджет и поддержка

- разработка MVP: 100 часов, 150 000 ₽ — плановая оценка;
- инфраструктура и API за год: 45 150 ₽ до резерва;
- поддержка и актуализация: 76 часов / 114 000 ₽ в год;
- первый год: 309 150 ₽ до резерва, около 355 500 ₽ с резервом 15%;
- последующие годы: 159 150 ₽ до резерва, около 183 000 ₽ с резервом.

Поддержка включает актуализацию базы знаний, квартальную проверку, поддержку пользователей, журналы и ошибки, контроль расходов, обновление компонентов и резервное копирование. Подробности и сценарии окупаемости приведены в [`COST_ESTIMATE_FINAL.md`](COST_ESTIMATE_FINAL.md).

## 15. Известные ограничения MVP

1. **Natural selection в mixed procurement.** После decomposition выбор
   отдельной позиции ограниченно понимает сокращённые и изменённые естественные
   формулировки. Обе заявки автоматически не создаются. Это P2.
2. **Unit lexicon.** Реестр единиц закрыт; отдельные формы, например
   `пар/пары`, могут потребовать явной нормализованной единицы. Это P2.
3. **Service clarification wording.** Некоторые разные поля услуги имеют очень
   похожие вопросы. Это UX/P2, а не подтверждённая потеря persistence.
4. **Границы taxonomy.** `intake-categories-v3` — синтетическая taxonomy MVP.
   Пограничные специализированные предметы могут потребовать clarification или
   подтверждения пользователя.
5. **LLM dependency.** Для предметов вне deterministic vocabulary category
   suggestion зависит от OpenAI provider. При ошибке категория безопасно
   остаётся unresolved.
6. **LLM latency.** Real category-provider smoke показывал задержки порядка
   нескольких секунд; фиксированный SLA не заявляется.
7. **Graceful shutdown.** В отдельных предыдущих deployment после остановки
   aiogram polling Python process не завершался до `TimeoutStopSec=30`, после
   чего systemd отправлял SIGKILL. Последующие остановки проходили штатно, но
   кодовое исправление этого поведения не зафиксировано.
8. Одна заявка хранит один основной предмет и один набор
   количества/единицы/характеристик; построчной спецификации нет.
9. Нет отдельного поля даты начала услуги, выбора редактируемого поля кнопкой и
   автоматической регистрации второй части mixed-потребности.
10. История ограничена пятью read-only записями; кабинета закупщика и
    фактического согласования нет.
11. Pending Regulation context обновляется неатомарно. Длительное
    многопользовательское, нагрузочное и отдельное security-тестирование не
    проводилось.
12. До реального пилота нужны RLS, формальная политика хранения, ротация
    секретов и проверка минимальных прав. Внешние цены, поставщики и реальные
    корпоративные данные не подключены.

Unsafe generic fallback G01–G04, потеря типа router/monitor, taxonomy gap
промышленного вентилятора, повторная переклассификация подтверждённой G04,
отсутствующий category intent в RAG, состояние индекса 117/118 и UUID relocation
defect устранены и не считаются текущими ограничениями.

## 16. Итог приёмки

MVP принят по результатам финального функционального и production smoke testing
с известными ограничениями. Он соответствует функциональному объёму выпускного
проекта: основные сценарии intake и Regulation Q&A реализованы, критические
дефекты rehearsal устранены, production deployment, синхронизация индекса и
финальная smoke-проверка завершены.

Оставшиеся ограничения относятся к P2, UX и operational scope и не блокируют
демонстрацию или защиту. Продукт не заявляется как готовая enterprise
production system. Перед реальным пилотом обязательны безопасность и
эксплуатационная готовность, независимые наборы на обезличенных реальных
формулировках и измерение исходных KPI.

Полная декомпозиция направлений развития находится в
[`../planning/PRODUCT_BACKLOG.md`](../planning/PRODUCT_BACKLOG.md).
